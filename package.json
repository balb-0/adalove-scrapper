{
  "name": "adalove-scrapper",
  "version": "0.1.0",
  "description": "Preparador de aula do Inteli — extension + web app open source",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "dev:web": "pnpm --filter web dev",
    "dev:ext": "pnpm --filter extension dev",
    "dev:ext:firefox": "pnpm --filter extension dev:firefox",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
